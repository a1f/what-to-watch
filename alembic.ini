# Alembic configuration. The database URL is intentionally NOT set here;
# api/alembic/env.py reads it from application settings (DATABASE_URL).
[alembic]
script_location = api/alembic
prepend_sys_path = .
path_separator = os
